package novalogics.android.canvasthemes.materialcanvas.themes.purpleblaze

import androidx.compose.ui.graphics.Color

object PurpleBlazeColors {
    object Light {
        val primary = Color(0xFF6200EA) // Primary color (Deep Purple)
        val onPrimary = Color(0xFFFFFFFF) // Content on primary (White)
        val primaryContainer = Color(0xFFEDE7F6) // Primary container
        val onPrimaryContainer = Color(0xFF21005D) // Content on primary container (Dark)
        val secondary = Color(0xFF7C4DFF) // Secondary color (Vivid Purple)
        val onSecondary = Color(0xFFFFFFFF) // Content on secondary (White)
        val secondaryContainer = Color(0xFFD1C4E9) // Secondary container
        val onSecondaryContainer = Color(0xFF311B92) // Content on secondary container (Dark)
        val tertiary = Color(0xFF512DA8) // Tertiary color (Dark Purple)
        val onTertiary = Color(0xFFFFFFFF) // Content on tertiary (White)
        val tertiaryContainer = Color(0xFFD1C4E9) // Tertiary container
        val onTertiaryContainer = Color(0xFF311B92) // Content on tertiary container (Dark)
        val error = Color(0xFFB00020) // Error color
        val errorContainer = Color(0xFFFFDAD6) // Error container
        val onError = Color(0xFFFFFFFF) // Content on error (White)
        val onErrorContainer = Color(0xFF410002) // Content on error container
        val background = Color(0xFFFFFBFE) // Background (Soft white)
        val onBackground = Color(0xFF1C1B1F) // Content on background (Dark)
        val surface = Color(0xFFF5F5F5) // Surface color
        val onSurface = Color(0xFF1C1B1F) // Content on surface (Dark)
        val surfaceVariant = Color(0xFFE1D5E8) // Surface variant
        val onSurfaceVariant = Color(0xFF49454F) // Content on surface variant
        val outline = Color(0xFF7E758C) // Outline color
        val inverseOnSurface = Color(0xFFF4EFF4) // Inverse content on surface
        val inverseSurface = Color(0xFF313033) // Inverse surface
        val inversePrimary = Color(0xFFBB86FC) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFF6200EA) // Surface tint
        val outlineVariant = Color(0xFFCBC0D7) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }

    object Dark {
        val primary = Color(0xFFBB86FC) // Primary color (Light Purple)
        val onPrimary = Color(0xFF3700B3) // Content on primary (Dark)
        val primaryContainer = Color(0xFF3700B3) // Primary container
        val onPrimaryContainer = Color(0xFFEDE7F6) // Content on primary container
        val secondary = Color(0xFFD1C4E9) // Secondary color (Light Purple)
        val onSecondary = Color(0xFF4A0072) // Content on secondary
        val secondaryContainer = Color(0xFF4A0072) // Secondary container
        val onSecondaryContainer = Color(0xFFD1C4E9) // Content on secondary container
        val tertiary = Color(0xFFB39DDB) // Tertiary color (Muted Purple)
        val onTertiary = Color(0xFF311B92) // Content on tertiary
        val tertiaryContainer = Color(0xFF4A0072) // Tertiary container
        val onTertiaryContainer = Color(0xFFD1C4E9) // Content on tertiary container
        val error = Color(0xFFFFB4A9) // Error color
        val errorContainer = Color(0xFF93000A) // Error container
        val onError = Color(0xFF680005) // Content on error
        val onErrorContainer = Color(0xFFFFDAD6) // Content on error container
        val background = Color(0xFF1C1B1F) // Background color
        val onBackground = Color(0xFFE6E1E5) // Content on background
        val surface = Color(0xFF2B2833) // Surface color
        val onSurface = Color(0xFFE6E1E5) // Content on surface
        val surfaceVariant = Color(0xFF49454F) // Surface variant
        val onSurfaceVariant = Color(0xFFCAC4D0) // Content on surface variant
        val outline = Color(0xFF938F99) // Outline color
        val inverseOnSurface = Color(0xFF1C1B1F) // Inverse content on surface
        val inverseSurface = Color(0xFFE6E1E5) // Inverse surface
        val inversePrimary = Color(0xFF6200EA) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFFBB86FC) // Surface tint
        val outlineVariant = Color(0xFF7E758C) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }
}
