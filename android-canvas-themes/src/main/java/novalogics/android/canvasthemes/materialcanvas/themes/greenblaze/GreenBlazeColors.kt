package novalogics.android.canvasthemes.materialcanvas.themes.greenblaze

import androidx.compose.ui.graphics.Color

object GreenBlazeColors {
    object Light {
        val primary = Color(0xFF388E3C) // Primary color (Green)
        val onPrimary = Color(0xFFFFFFFF) // Content on primary (White)
        val primaryContainer = Color(0xFFC8E6C9) // Primary container
        val onPrimaryContainer = Color(0xFF002411) // Content on primary container (Dark)
        val secondary = Color(0xFF43A047) // Secondary color (Lighter Green)
        val onSecondary = Color(0xFFFFFFFF) // Content on secondary (White)
        val secondaryContainer = Color(0xFFA5D6A7) // Secondary container
        val onSecondaryContainer = Color(0xFF002411) // Content on secondary container (Dark)
        val tertiary = Color(0xFF2E7D32) // Tertiary color (Darker Green)
        val onTertiary = Color(0xFFFFFFFF) // Content on tertiary (White)
        val tertiaryContainer = Color(0xFF81C784) // Tertiary container
        val onTertiaryContainer = Color(0xFF002411) // Content on tertiary container (Dark)
        val error = Color(0xFFB00020) // Error color
        val errorContainer = Color(0xFFFFDAD6) // Error container
        val onError = Color(0xFFFFFFFF) // Content on error (White)
        val onErrorContainer = Color(0xFF410002) // Content on error container
        val background = Color(0xFFF1F8E9) // Background (Soft green)
        val onBackground = Color(0xFF1B211F) // Content on background (Dark)
        val surface = Color(0xFFE8F5E9) // Surface color
        val onSurface = Color(0xFF1B211F) // Content on surface (Dark)
        val surfaceVariant = Color(0xFFC8E6C9) // Surface variant
        val onSurfaceVariant = Color(0xFF37413A) // Content on surface variant
        val outline = Color(0xFF8D9A91) // Outline color
        val inverseOnSurface = Color(0xFFF1F8E9) // Inverse content on surface
        val inverseSurface = Color(0xFF2B322E) // Inverse surface
        val inversePrimary = Color(0xFFA5D6A7) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFF388E3C) // Surface tint
        val outlineVariant = Color(0xFFC8E6C9) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }

    object Dark {
        val primary = Color(0xFFA5D6A7) // Primary color (Light Green)
        val onPrimary = Color(0xFF003311) // Content on primary (Dark)
        val primaryContainer = Color(0xFF004D26) // Primary container
        val onPrimaryContainer = Color(0xFFC8E6C9) // Content on primary container
        val secondary = Color(0xFF81C784) // Secondary color (Green)
        val onSecondary = Color(0xFF003311) // Content on secondary
        val secondaryContainer = Color(0xFF004D26) // Secondary container
        val onSecondaryContainer = Color(0xFFC8E6C9) // Content on secondary container
        val tertiary = Color(0xFF66BB6A) // Tertiary color (Green)
        val onTertiary = Color(0xFF003311) // Content on tertiary
        val tertiaryContainer = Color(0xFF004D26) // Tertiary container
        val onTertiaryContainer = Color(0xFFC8E6C9) // Content on tertiary container
        val error = Color(0xFFFFB4A9) // Error color
        val errorContainer = Color(0xFF93000A) // Error container
        val onError = Color(0xFF680005) // Content on error
        val onErrorContainer = Color(0xFFFFDAD6) // Content on error container
        val background = Color(0xFF1B211F) // Background color
        val onBackground = Color(0xFFE1E6E0) // Content on background
        val surface = Color(0xFF26332E) // Surface color
        val onSurface = Color(0xFFE1E6E0) // Content on surface
        val surfaceVariant = Color(0xFF37413A) // Surface variant
        val onSurfaceVariant = Color(0xFFBFC9BE) // Content on surface variant
        val outline = Color(0xFF8D9A91) // Outline color
        val inverseOnSurface = Color(0xFF1B211F) // Inverse content on surface
        val inverseSurface = Color(0xFFE1E6E0) // Inverse surface
        val inversePrimary = Color(0xFF388E3C) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFFA5D6A7) // Surface tint
        val outlineVariant = Color(0xFF4C564F) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }
}
