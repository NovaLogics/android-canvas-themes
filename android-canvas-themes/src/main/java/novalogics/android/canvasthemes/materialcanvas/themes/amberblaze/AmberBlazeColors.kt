package novalogics.android.canvasthemes.materialcanvas.themes.amberblaze

import androidx.compose.ui.graphics.Color

object AmberBlazeColors {
    object Light {
        val primary = Color(0xFFFFC107) // Primary color (Amber)
        val onPrimary = Color(0xFF000000) // Content on primary (Black)
        val primaryContainer = Color(0xFFFFECB3) // Primary container
        val onPrimaryContainer = Color(0xFF332500) // Content on primary container (Dark)
        val secondary = Color(0xFFFFD54F) // Secondary color (Lighter Amber)
        val onSecondary = Color(0xFF000000) // Content on secondary (Black)
        val secondaryContainer = Color(0xFFFFF3E0) // Secondary container
        val onSecondaryContainer = Color(0xFF332500) // Content on secondary container (Dark)
        val tertiary = Color(0xFFFFA000) // Tertiary color (Darker Amber)
        val onTertiary = Color(0xFFFFFFFF) // Content on tertiary (White)
        val tertiaryContainer = Color(0xFFFFD740) // Tertiary container
        val onTertiaryContainer = Color(0xFF332500) // Content on tertiary container (Dark)
        val error = Color(0xFFB00020) // Error color
        val errorContainer = Color(0xFFFFDAD6) // Error container
        val onError = Color(0xFFFFFFFF) // Content on error (White)
        val onErrorContainer = Color(0xFF410002) // Content on error container
        val background = Color(0xFFFFF8E1) // Background (Soft amber)
        val onBackground = Color(0xFF1C1B17) // Content on background (Dark)
        val surface = Color(0xFFFFF3E0) // Surface color
        val onSurface = Color(0xFF1C1B17) // Content on surface (Dark)
        val surfaceVariant = Color(0xFFFFE082) // Surface variant
        val onSurfaceVariant = Color(0xFF4F4335) // Content on surface variant
        val outline = Color(0xFF8D6E63) // Outline color
        val inverseOnSurface = Color(0xFFFFF8E1) // Inverse content on surface
        val inverseSurface = Color(0xFF332500) // Inverse surface
        val inversePrimary = Color(0xFFFFA000) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFFFFC107) // Surface tint
        val outlineVariant = Color(0xFFFFECB3) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }

    object Dark {
        val primary = Color(0xFFFFA000) // Primary color (Dark Amber)
        val onPrimary = Color(0xFF332500) // Content on primary (Dark)
        val primaryContainer = Color(0xFF4F2D00) // Primary container
        val onPrimaryContainer = Color(0xFFFFECB3) // Content on primary container
        val secondary = Color(0xFFFFC107) // Secondary color (Amber)
        val onSecondary = Color(0xFF332500) // Content on secondary
        val secondaryContainer = Color(0xFF4F2D00) // Secondary container
        val onSecondaryContainer = Color(0xFFFFECB3) // Content on secondary container
        val tertiary = Color(0xFFFFD54F) // Tertiary color (Lighter Amber)
        val onTertiary = Color(0xFF332500) // Content on tertiary
        val tertiaryContainer = Color(0xFF4F2D00) // Tertiary container
        val onTertiaryContainer = Color(0xFFFFECB3) // Content on tertiary container
        val error = Color(0xFFFFB4A9) // Error color
        val errorContainer = Color(0xFF93000A) // Error container
        val onError = Color(0xFF680005) // Content on error
        val onErrorContainer = Color(0xFFFFDAD6) // Content on error container
        val background = Color(0xFF1C1B17) // Background color
        val onBackground = Color(0xFFEDE0D4) // Content on background
        val surface = Color(0xFF332500) // Surface color
        val onSurface = Color(0xFFEDE0D4) // Content on surface
        val surfaceVariant = Color(0xFF4F4335) // Surface variant
        val onSurfaceVariant = Color(0xFFDAC5B1) // Content on surface variant
        val outline = Color(0xFF8D6E63) // Outline color
        val inverseOnSurface = Color(0xFF1C1B17) // Inverse content on surface
        val inverseSurface = Color(0xFFEDE0D4) // Inverse surface
        val inversePrimary = Color(0xFFFFC107) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFFFFA000) // Surface tint
        val outlineVariant = Color(0xFF755845) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }
}
