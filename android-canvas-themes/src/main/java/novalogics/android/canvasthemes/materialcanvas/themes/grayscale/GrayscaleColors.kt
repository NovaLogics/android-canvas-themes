package novalogics.android.canvasthemes.materialcanvas.themes.grayscale

import androidx.compose.ui.graphics.Color

object GrayscaleColors {

    object Light {
        val primary = Color(0xFF000000) // Primary color (Black)
        val onPrimary = Color(0xFFFFFFFF) // On Primary (white for contrast)
        val primaryContainer = Color(0xFFE0E0E0) // Primary Container (Light Gray)
        val onPrimaryContainer = Color(0xFF3D3D3D) // Dark text on Primary Container
        val secondary = Color(0xFF757575) // Lighter Secondary (Gray)
        val onSecondary = Color(0xFFFFFFFF) // On Secondary (white)
        val secondaryContainer = Color(0xFFBDBDBD) // Secondary Container
        val onSecondaryContainer = Color(0xFF3D3D3D) // Dark text on Secondary Container
        val tertiary = Color(0xFF616161) // Tertiary (Dark Gray)
        val onTertiary = Color(0xFFFFFFFF) // On Tertiary (white)
        val tertiaryContainer = Color(0xFFBDBDBD) // Tertiary Container
        val onTertiaryContainer = Color(0xFF3D3D3D) // Dark text on Tertiary Container
        val error = Color(0xFFB71C1C) // Error (Dark Red)
        val errorContainer = Color(0xFFF8BBD0) // Error Container (Light Pink)
        val onError = Color(0xFFFFFFFF) // On Error (white)
        val onErrorContainer = Color(0xFF410002) // On Error Container
        val background = Color(0xFFFFFFFF) // Light Background (White)
        val onBackground = Color(0xFF000000) // On Background (Black)
        val surface = Color(0xFFF5F5F5) // Surface (Light Gray)
        val onSurface = Color(0xFF000000) // On Surface (Black)
        val surfaceVariant = Color(0xFFEEEEEE) // Surface Variant (Very Light Gray)
        val onSurfaceVariant = Color(0xFF3D3D3D) // On Surface Variant
        val outline = Color(0xFF9E9E9E) // Outline (Gray)
        val inverseOnSurface = Color(0xFF000000) // Inverse On Surface
        val inverseSurface = Color(0xFFBDBDBD) // Inverse Surface
        val inversePrimary = Color(0xFF000000) // Inverse Primary
        val shadow = Color(0xFF000000) // Shadow
        val surfaceTint = Color(0xFF000000) // Surface Tint
        val outlineVariant = Color(0xFFBDBDBD) // Outline Variant
        val scrim = Color(0xFF000000) // Scrim
    }

    object Dark {
        val primary = Color(0xFF000000) // Primary Dark (Black)
        val onPrimary = Color(0xFFFFFFFF) // On Primary (white)
        val primaryContainer = Color(0xFF3E3E3E) // Darker container for contrast
        val onPrimaryContainer = Color(0xFFE0E0E0) // On Primary Container (Light Gray)
        val secondary = Color(0xFF616161) // Secondary Gray
        val onSecondary = Color(0xFF000000) // On Secondary (Black)
        val secondaryContainer = Color(0xFF424242) // Secondary Container (Darker Gray)
        val onSecondaryContainer = Color(0xFFE0E0E0) // On Secondary Container
        val tertiary = Color(0xFF424242) // Tertiary Darker Gray
        val onTertiary = Color(0xFFFFFFFF) // On Tertiary (white)
        val tertiaryContainer = Color(0xFF616161) // Tertiary Container
        val onTertiaryContainer = Color(0xFFF5F5F5) // On Tertiary Container
        val error = Color(0xFFEF5350) // Error (Light Red)
        val errorContainer = Color(0xFFB71C1C) // Error Container
        val onError = Color(0xFF000000) // On Error (Black)
        val onErrorContainer = Color(0xFFFFCDD2) // On Error Container (Light Red)
        val background = Color(0xFF212121) // Dark Background (Dark Gray)
        val onBackground = Color(0xFFE0E0E0) // On Background (Light Gray)
        val surface = Color(0xFF303030) // Surface (Dark Gray)
        val onSurface = Color(0xFFE0E0E0) // On Surface (Light Gray)
        val surfaceVariant = Color(0xFF616161) // Surface Variant
        val onSurfaceVariant = Color(0xFFBDBDBD) // On Surface Variant
        val outline = Color(0xFF616161) // Outline
        val inverseOnSurface = Color(0xFF000000) // Inverse On Surface
        val inverseSurface = Color(0xFFE0E0E0) // Inverse Surface
        val inversePrimary = Color(0xFFE0E0E0) // Inverse Primary (Light Gray)
        val shadow = Color(0xFF000000) // Shadow
        val surfaceTint = Color(0xFF000000) // Surface Tint
        val outlineVariant = Color(0xFF616161) // Outline Variant
        val scrim = Color(0xFF000000) // Scrim
    }
}
