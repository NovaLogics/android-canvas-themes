package novalogics.android.canvasthemes.materialcanvas.themes.redblaze

import androidx.compose.ui.graphics.Color

object RedBlazeColors {
    object Light {
        val primary = Color(0xFFB71C1C) // Primary color (Deep Red)
        val onPrimary = Color(0xFFFFFFFF) // Content on primary (White)
        val primaryContainer = Color(0xFFFFCDD2) // Primary container
        val onPrimaryContainer = Color(0xFF370000) // Content on primary container (Dark)
        val secondary = Color(0xFFD32F2F) // Secondary color (Red)
        val onSecondary = Color(0xFFFFFFFF) // Content on secondary (White)
        val secondaryContainer = Color(0xFFFFEBEE) // Secondary container
        val onSecondaryContainer = Color(0xFF440000) // Content on secondary container (Dark)
        val tertiary = Color(0xFFE53935) // Tertiary color (Bright Red)
        val onTertiary = Color(0xFFFFFFFF) // Content on tertiary (White)
        val tertiaryContainer = Color(0xFFFFEBEE) // Tertiary container
        val onTertiaryContainer = Color(0xFF5D0000) // Content on tertiary container (Dark)
        val error = Color(0xFFB00020) // Error color
        val errorContainer = Color(0xFFFFDAD6) // Error container
        val onError = Color(0xFFFFFFFF) // Content on error (White)
        val onErrorContainer = Color(0xFF410002) // Content on error container
        val background = Color(0xFFFFFBF9) // Background (Soft cream)
        val onBackground = Color(0xFF1E1E1E) // Content on background (Dark)
        val surface = Color(0xFFFFEAEA) // Surface color
        val onSurface = Color(0xFF1E1E1E) // Content on surface (Dark)
        val surfaceVariant = Color(0xFFEF9A9A) // Surface variant
        val onSurfaceVariant = Color(0xFF5A0000) // Content on surface variant
        val outline = Color(0xFF8D4D4D) // Outline color
        val inverseOnSurface = Color(0xFFFCEEEF) // Inverse content on surface
        val inverseSurface = Color(0xFF5A1A1A) // Inverse surface
        val inversePrimary = Color(0xFFFF8A80) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFFB71C1C) // Surface tint
        val outlineVariant = Color(0xFFE57373) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }

    object Dark {
        val primary = Color(0xFFFF8A80) // Primary color (Light Red)
        val onPrimary = Color(0xFF680003) // Content on primary (Dark)
        val primaryContainer = Color(0xFF930003) // Primary container
        val onPrimaryContainer = Color(0xFFFFEBEE) // Content on primary container
        val secondary = Color(0xFFFFB4A9) // Secondary color (Pinkish Red)
        val onSecondary = Color(0xFF680003) // Content on secondary
        val secondaryContainer = Color(0xFF930003) // Secondary container
        val onSecondaryContainer = Color(0xFFFFEBEE) // Content on secondary container
        val tertiary = Color(0xFFFF8A80) // Tertiary color (Light Red)
        val onTertiary = Color(0xFF680003) // Content on tertiary
        val tertiaryContainer = Color(0xFF930003) // Tertiary container
        val onTertiaryContainer = Color(0xFFFFEAEA) // Content on tertiary container
        val error = Color(0xFFFFB4A9) // Error color
        val errorContainer = Color(0xFF93000A) // Error container
        val onError = Color(0xFF680005) // Content on error
        val onErrorContainer = Color(0xFFFFDAD6) // Content on error container
        val background = Color(0xFF1E1E1E) // Background color
        val onBackground = Color(0xFFF8DAD7) // Content on background
        val surface = Color(0xFF2A2A2A) // Surface color
        val onSurface = Color(0xFFE0C7C7) // Content on surface
        val surfaceVariant = Color(0xFF4E3D3D) // Surface variant
        val onSurfaceVariant = Color(0xFFDEB6B6) // Content on surface variant
        val outline = Color(0xFF9A6060) // Outline color
        val inverseOnSurface = Color(0xFF1E1E1E) // Inverse content on surface
        val inverseSurface = Color(0xFFF8DAD7) // Inverse surface
        val inversePrimary = Color(0xFFB71C1C) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFFFF8A80) // Surface tint
        val outlineVariant = Color(0xFF725252) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }
}

