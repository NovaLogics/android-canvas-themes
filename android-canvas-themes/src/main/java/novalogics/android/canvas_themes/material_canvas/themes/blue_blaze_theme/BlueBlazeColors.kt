package novalogics.android.canvas_themes.material_canvas.themes.blue_blaze_theme

import androidx.compose.ui.graphics.Color

object BlueBlazeColors {
    object Light {
        val primary = Color(0xFF1976D2) // Primary color (Blue)
        val onPrimary = Color(0xFFFFFFFF) // Content on primary (White)
        val primaryContainer = Color(0xFFBBDEFB) // Primary container
        val onPrimaryContainer = Color(0xFF001A34) // Content on primary container (Dark)
        val secondary = Color(0xFF2196F3) // Secondary color (Lighter Blue)
        val onSecondary = Color(0xFFFFFFFF) // Content on secondary (White)
        val secondaryContainer = Color(0xFF90CAF9) // Secondary container
        val onSecondaryContainer = Color(0xFF002952) // Content on secondary container (Dark)
        val tertiary = Color(0xFF0D47A1) // Tertiary color (Darker Blue)
        val onTertiary = Color(0xFFFFFFFF) // Content on tertiary (White)
        val tertiaryContainer = Color(0xFF64B5F6) // Tertiary container
        val onTertiaryContainer = Color(0xFF001A34) // Content on tertiary container (Dark)
        val error = Color(0xFFB00020) // Error color
        val errorContainer = Color(0xFFFFDAD6) // Error container
        val onError = Color(0xFFFFFFFF) // Content on error (White)
        val onErrorContainer = Color(0xFF410002) // Content on error container
        val background = Color(0xFFE3F2FD) // Background (Soft blue)
        val onBackground = Color(0xFF1C1F1E) // Content on background (Dark)
        val surface = Color(0xFFBBDEFB) // Surface color
        val onSurface = Color(0xFF1C1F1E) // Content on surface (Dark)
        val surfaceVariant = Color(0xFF90CAF9) // Surface variant
        val onSurfaceVariant = Color(0xFF37474F) // Content on surface variant
        val outline = Color(0xFF78909C) // Outline color
        val inverseOnSurface = Color(0xFFE3F2FD) // Inverse content on surface
        val inverseSurface = Color(0xFF2E3B4A) // Inverse surface
        val inversePrimary = Color(0xFF64B5F6) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFF1976D2) // Surface tint
        val outlineVariant = Color(0xFFBBDEFB) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }

    object Dark {
        val primary = Color(0xFF64B5F6) // Primary color (Light Blue)
        val onPrimary = Color(0xFF002A5C) // Content on primary (Dark)
        val primaryContainer = Color(0xFF003C8F) // Primary container
        val onPrimaryContainer = Color(0xFFBBDEFB) // Content on primary container
        val secondary = Color(0xFF90CAF9) // Secondary color (Light Blue)
        val onSecondary = Color(0xFF002952) // Content on secondary
        val secondaryContainer = Color(0xFF003C8F) // Secondary container
        val onSecondaryContainer = Color(0xFFBBDEFB) // Content on secondary container
        val tertiary = Color(0xFF42A5F5) // Tertiary color (Blue)
        val onTertiary = Color(0xFF002A5C) // Content on tertiary
        val tertiaryContainer = Color(0xFF003C8F) // Tertiary container
        val onTertiaryContainer = Color(0xFFBBDEFB) // Content on tertiary container
        val error = Color(0xFFFFB4A9) // Error color
        val errorContainer = Color(0xFF93000A) // Error container
        val onError = Color(0xFF680005) // Content on error
        val onErrorContainer = Color(0xFFFFDAD6) // Content on error container
        val background = Color(0xFF1C1F1E) // Background color
        val onBackground = Color(0xFFDDE1E7) // Content on background
        val surface = Color(0xFF263238) // Surface color
        val onSurface = Color(0xFFDDE1E7) // Content on surface
        val surfaceVariant = Color(0xFF37474F) // Surface variant
        val onSurfaceVariant = Color(0xFFBFC9D4) // Content on surface variant
        val outline = Color(0xFF78909C) // Outline color
        val inverseOnSurface = Color(0xFF1C1F1E) // Inverse content on surface
        val inverseSurface = Color(0xFFDDE1E7) // Inverse surface
        val inversePrimary = Color(0xFF1976D2) // Inverse primary
        val shadow = Color(0xFF000000) // Shadow color
        val surfaceTint = Color(0xFF64B5F6) // Surface tint
        val outlineVariant = Color(0xFF546E7A) // Outline variant
        val scrim = Color(0xFF000000) // Scrim color
    }
}
