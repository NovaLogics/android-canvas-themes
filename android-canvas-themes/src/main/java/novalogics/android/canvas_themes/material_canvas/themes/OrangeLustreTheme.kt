package novalogics.android.canvas_themes.material_canvas.themes

import androidx.compose.ui.graphics.Color

object OrangeLustreTheme {
    object Light {
        val primary = Color(0xFFFF9800) // Primary color (Orange)
        val onPrimary = Color(0xFFFFFFFF) // On Primary (white for contrast)
        val primaryContainer = Color(0xFFFFC107) // Primary Container
        val onPrimaryContainer = Color(0xFF3D3D3D) // Dark text on Primary Container
        val secondary = Color(0xFFFFA726) // Lighter Secondary (Orange)
        val onSecondary = Color(0xFFFFFFFF) // On Secondary (white)
        val secondaryContainer = Color(0xFFFFD54F) // Secondary Container
        val onSecondaryContainer = Color(0xFF3D3D3D) // Dark text on Secondary Container
        val tertiary = Color(0xFFFB8C00) // Tertiary (Darker Orange)
        val onTertiary = Color(0xFFFFFFFF) // On Tertiary (white)
        val tertiaryContainer = Color(0xFFFFE57F) // Tertiary Container
        val onTertiaryContainer = Color(0xFF3D3D3D) // Dark text on Tertiary Container
        val error = Color(0xFFBA1A1A) // Error
        val errorContainer = Color(0xFFFFDAD6) // Error Container
        val onError = Color(0xFFFFFFFF) // On Error (white)
        val onErrorContainer = Color(0xFF410002) // On Error Container
        val background = Color(0xFFFDFCFB) // Light Background (Soft Cream)
        val onBackground = Color(0xFF191C1A) // On Background (dark)
        val surface = Color(0xFFFFF3E0) // Surface
        val onSurface = Color(0xFF191C1A) // On Surface (dark)
        val surfaceVariant = Color(0xFFDBE5DD) // Surface Variant
        val onSurfaceVariant = Color(0xFF404943) // On Surface Variant
        val outline = Color(0xFF707973) // Outline
        val inverseOnSurface = Color(0xFFEFF1ED) // Inverse On Surface
        val inverseSurface = Color(0xFF2E312F) // Inverse Surface
        val inversePrimary = Color(0xFF6CDBAC) // Inverse Primary
        val shadow = Color(0xFF000000) // Shadow
        val surfaceTint = Color(0xFF006C4C) // Surface Tint
        val outlineVariant = Color(0xFFBFC9C2) // Outline Variant
        val scrim = Color(0xFF000000) // Scrim
    }

    object Dark {
        val primary = Color(0xFFF57C00) // Primary Dark Orange
        val onPrimary = Color(0xFFFFFFFF) // On Primary (white)
        val primaryContainer = Color(0xFF3E2723) // Darker container for contrast
        val onPrimaryContainer = Color(0xFFFFE0B2) // On Primary Container
        val secondary = Color(0xFFFFA726) // Secondary Orange
        val onSecondary = Color(0xFF1F352A) // On Secondary
        val secondaryContainer = Color(0xFF354B40) // Secondary Container
        val onSecondaryContainer = Color(0xFFFCF4E8) // On Secondary Container
        val tertiary = Color(0xFFFB8C00) // Tertiary Darker Orange
        val onTertiary = Color(0xFFFFFFFF) // On Tertiary (white)
        val tertiaryContainer = Color(0xFF3E2723) // Tertiary Container
        val onTertiaryContainer = Color(0xFFFFE57F) // On Tertiary Container
        val error = Color(0xFFFFB4AB) // Error
        val errorContainer = Color(0xFF93000A) // Error Container
        val onError = Color(0xFF690005) // On Error
        val onErrorContainer = Color(0xFFFFDAD6) // On Error Container
        val background = Color(0xFF191C1A) // Background
        val onBackground = Color(0xFFE1E3DF) // On Background
        val surface = Color(0xFF191C1A) // Surface
        val onSurface = Color(0xFFE1E3DF) // On Surface
        val surfaceVariant = Color(0xFF404943) // Surface Variant
        val onSurfaceVariant = Color(0xFFBFC9C2) // On Surface Variant
        val outline = Color(0xFF8A938C) // Outline
        val inverseOnSurface = Color(0xFF191C1A) // Inverse On Surface
        val inverseSurface = Color(0xFFE1E3DF) // Inverse Surface
        val inversePrimary = Color(0xFF006C4C) // Inverse Primary
        val shadow = Color(0xFF000000) // Shadow
        val surfaceTint = Color(0xFF6CDBAC) // Surface Tint
        val outlineVariant = Color(0xFF404943) // Outline Variant
        val scrim = Color(0xFF000000) // Scrim
    }
}
