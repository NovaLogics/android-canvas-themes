package novalogics.android.canvas_themes_library.themes

import androidx.compose.ui.graphics.Color

object OrangeBlazeTheme {
    object Light {
        val Primary = Color(0xFFFF9800) // Primary color (Orange)
        val OnPrimary = Color(0xFFFFFFFF) // Content on primary (White)
        val PrimaryContainer = Color(0xFFFFC107) // Primary container
        val OnPrimaryContainer = Color(0xFF3D3D3D) // Content on primary container (Dark)
        val Secondary = Color(0xFFFFA726) // Secondary color (Lighter Orange)
        val OnSecondary = Color(0xFFFFFFFF) // Content on secondary (White)
        val SecondaryContainer = Color(0xFFFFD54F) // Secondary container
        val OnSecondaryContainer = Color(0xFF3D3D3D) // Content on secondary container (Dark)
        val Tertiary = Color(0xFFFB8C00) // Tertiary color (Darker Orange)
        val OnTertiary = Color(0xFFFFFFFF) // Content on tertiary (White)
        val TertiaryContainer = Color(0xFFFFE57F) // Tertiary container
        val OnTertiaryContainer = Color(0xFF3D3D3D) // Content on tertiary container (Dark)
        val Error = Color(0xFFBA1A1A) // Error color
        val ErrorContainer = Color(0xFFFFDAD6) // Error container
        val OnError = Color(0xFFFFFFFF) // Content on error (White)
        val OnErrorContainer = Color(0xFF410002) // Content on error container
        val Background = Color(0xFFFDFCFB) // Background (Soft cream)
        val OnBackground = Color(0xFF191C1A) // Content on background (Dark)
        val Surface = Color(0xFFFFF3E0) // Surface color
        val OnSurface = Color(0xFF191C1A) // Content on surface (Dark)
        val SurfaceVariant = Color(0xFFDBE5DD) // Surface variant
        val OnSurfaceVariant = Color(0xFF404943) // Content on surface variant
        val Outline = Color(0xFF707973) // Outline color
        val InverseOnSurface = Color(0xFFEFF1ED) // Inverse content on surface
        val InverseSurface = Color(0xFF2E312F) // Inverse surface
        val InversePrimary = Color(0xFF6CDBAC) // Inverse primary
        val Shadow = Color(0xFF000000) // Shadow color
        val SurfaceTint = Color(0xFF006C4C) // Surface tint
        val OutlineVariant = Color(0xFFBFC9C2) // Outline variant
        val Scrim = Color(0xFF000000) // Scrim color
    }

    object Dark {
        val Primary = Color(0xFFF57C00) // Primary color (Dark orange)
        val OnPrimary = Color(0xFFFFFFFF) // Content on primary (White)
        val PrimaryContainer = Color(0xFF3E2723) // Primary container
        val OnPrimaryContainer = Color(0xFFFFE0B2) // Content on primary container
        val Secondary = Color(0xFFFFA726) // Secondary color (Orange)
        val OnSecondary = Color(0xFF1F352A) // Content on secondary
        val SecondaryContainer = Color(0xFF354B40) // Secondary container
        val OnSecondaryContainer = Color(0xFFFCF4E8) // Content on secondary container
        val Tertiary = Color(0xFFFB8C00) // Tertiary color (Dark orange)
        val OnTertiary = Color(0xFFFFFFFF) // Content on tertiary (White)
        val TertiaryContainer = Color(0xFF3E2723) // Tertiary container
        val OnTertiaryContainer = Color(0xFFFFE57F) // Content on tertiary container
        val Error = Color(0xFFFFB4AB) // Error color
        val ErrorContainer = Color(0xFF93000A) // Error container
        val OnError = Color(0xFF690005) // Content on error
        val OnErrorContainer = Color(0xFFFFDAD6) // Content on error container
        val Background = Color(0xFF191C1A) // Background color
        val OnBackground = Color(0xFFE1E3DF) // Content on background
        val Surface = Color(0xFF191C1A) // Surface color
        val OnSurface = Color(0xFFE1E3DF) // Content on surface
        val SurfaceVariant = Color(0xFF404943) // Surface variant
        val OnSurfaceVariant = Color(0xFFBFC9C2) // Content on surface variant
        val Outline = Color(0xFF8A938C) // Outline color
        val InverseOnSurface = Color(0xFF191C1A) // Inverse content on surface
        val InverseSurface = Color(0xFFE1E3DF) // Inverse surface
        val InversePrimary = Color(0xFF006C4C) // Inverse primary
        val Shadow = Color(0xFF000000) // Shadow color
        val SurfaceTint = Color(0xFF6CDBAC) // Surface tint
        val OutlineVariant = Color(0xFF404943) // Outline variant
        val Scrim = Color(0xFF000000) // Scrim color
    }
}
